
# Create a `Stack` class that stores elements
# It should have a `size` method that returns number of elements it has (len nélkül az elemek száma)
# It should have a `push` method that adds an element to the stack (hozzáadás append nélkül)
# It should have a `pop` method that returns the last element form the stack and also deletes it from it ()

# please don`t use the built in methods

class Stack():
